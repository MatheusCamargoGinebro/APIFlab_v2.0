// O========================================================================================O

/*
    O===================================O
    |   Controllers do objeto campus    |
    O===================================O

    Lista de funções:  
    - [x] get_campus_list;
    - [x] register_new_campus;
*/

// O========================================================================================O

// Importando os módulos necessários:
const Campus = require("../models/campus_model");

// O========================================================================================O

// Função para obter a lista de campi:
const get_campus_list = async (request, response) => {};

// Função para registrar um novo campus:
const register_new_campus = async (request, response) => {}

// O========================================================================================O

module.exports = {
  get_campus_list,
  register_new_campus,
};

// O========================================================================================O
